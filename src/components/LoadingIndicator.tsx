import { Box, Typography } from '@mui/material';

const LoadingIndicator: React.FC = () => (
  <Box sx={{ position: 'fixed', top: '50%', left: '50%', transform: 'translate(-50%, -50%)', fontSize: '2rem' }}>
    ...
  </Box>
);

export default LoadingIndicator;
